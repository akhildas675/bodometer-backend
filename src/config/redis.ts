/**
 * Redis configuration stub.
 * Redis dependency has been replaced by Stateless JWT HttpOnly Cookies and MongoDB TTL collections.
 */

export const connectRedis = async (): Promise<void> => {
  return Promise.resolve();
};
